import { Extraapplicantinfo } from './extraapplicantinfo';

describe('Extraapplicantinfo', () => {
  it('should create an instance', () => {
    expect(new Extraapplicantinfo()).toBeTruthy();
  });
});
