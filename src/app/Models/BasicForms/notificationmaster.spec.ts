import { Notificationmaster } from './notificationmaster';

describe('Notificationmaster', () => {
  it('should create an instance', () => {
    expect(new Notificationmaster()).toBeTruthy();
  });
});
