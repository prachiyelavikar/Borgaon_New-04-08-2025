import { CashCreditAddressDetails } from './cash-credit-address-details';

describe('CashCreditAddressDetails', () => {
  it('should create an instance', () => {
    expect(new CashCreditAddressDetails()).toBeTruthy();
  });
});
