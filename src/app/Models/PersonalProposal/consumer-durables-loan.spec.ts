import { CONSUMERDURABLESLOAN } from './consumer-durables-loan';

describe('CONSUMERDURABLESLOAN', () => {
  it('should create an instance', () => {
    expect(new CONSUMERDURABLESLOAN()).toBeTruthy();
  });
});
