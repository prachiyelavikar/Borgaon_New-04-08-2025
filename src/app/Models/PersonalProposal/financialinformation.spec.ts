import { Financialinformation } from './financialinformation';

describe('Financialinformation', () => {
  it('should create an instance', () => {
    expect(new Financialinformation()).toBeTruthy();
  });
});
