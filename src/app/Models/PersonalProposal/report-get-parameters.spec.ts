import { ReportGetParameters } from './report-get-parameters';

describe('ReportGetParameters', () => {
  it('should create an instance', () => {
    expect(new ReportGetParameters()).toBeTruthy();
  });
});
